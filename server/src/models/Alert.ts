import { Schema, Document, model } from 'mongoose';\n\nexport interface IAlert extends Document {\n  _id: string;\n  userId: string;\n  productId: string;\n  targetPrice: number;\n  active: boolean;\n  notified: boolean;\n  notifiedAt?: Date;\n  createdAt: Date;\n  updatedAt: Date;\n}\n\nconst alertSchema = new Schema<IAlert>(\n  {\n    userId: {\n      type: Schema.Types.ObjectId,\n      ref: 'User',\n      required: true,\n      index: true,\n    },\n    productId: {\n      type: Schema.Types.ObjectId,\n      ref: 'Product',\n      required: true,\n      index: true,\n    },\n    targetPrice: {\n      type: Number,\n      required: [true, 'Target price is required'],\n      min: 0,\n    },\n    active: {\n      type: Boolean,\n      default: true,\n    },\n    notified: {\n      type: Boolean,\n      default: false,\n    },\n    notifiedAt: Date,\n  },\n  {\n    timestamps: true,\n  }\n);\n\n// Compound index\nalertSchema.index({ userId: 1, productId: 1 });\nalertSchema.index({ active: 1, userId: 1 });\n\nexport const Alert = model<IAlert>('Alert', alertSchema);\n