import { Schema, Document, model } from 'mongoose';\n\nexport interface IPrice extends Document {\n  _id: string;\n  productId: string;\n  storeId: string;\n  price: number;\n  offerPrice?: number;\n  savings?: number;\n  source: 'bill_upload' | 'user_submission' | 'affiliate_feed' | 'retailer_api';\n  lastUpdated: Date;\n  externalId?: string;\n  createdAt: Date;\n  updatedAt: Date;\n}\n\nconst priceSchema = new Schema<IPrice>(\n  {\n    productId: {\n      type: Schema.Types.ObjectId,\n      ref: 'Product',\n      required: true,\n      index: true,\n    },\n    storeId: {\n      type: Schema.Types.ObjectId,\n      ref: 'Store',\n      required: true,\n      index: true,\n    },\n    price: {\n      type: Number,\n      required: [true, 'Price is required'],\n      min: 0,\n    },\n    offerPrice: {\n      type: Number,\n      min: 0,\n    },\n    savings: {\n      type: Number,\n      default: 0,\n    },\n    source: {\n      type: String,\n      enum: ['bill_upload', 'user_submission', 'affiliate_feed', 'retailer_api'],\n      default: 'retailer_api',\n    },\n    lastUpdated: {\n      type: Date,\n      default: Date.now,\n    },\n    externalId: String,\n  },\n  {\n    timestamps: true,\n  }\n);\n\n// Compound index for product and store\npriceSchema.index({ productId: 1, storeId: 1 });\npriceSchema.index({ lastUpdated: -1 });\npriceSchema.index({ price: 1 });\n\nexport const Price = model<IPrice>('Price', priceSchema);\n